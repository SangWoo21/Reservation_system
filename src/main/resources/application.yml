spring:
  datasource:
    url: jdbc:mysql://reservation.c9iqgkqq2vql.ap-southeast-2.rds.amazonaws.com:3306/ticketing?createDatabaseIfNotExist=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: reservation123!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 3000

  jpa:
    hibernate:
      ddl-auto: update        # 처음엔 update로 (테이블 자동 생성)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true            # SQL 콘솔에 출력

  lifecycle:
    timeout-per-shutdown-phase: 270s

server:
  port: 8080
  shutdown: graceful
